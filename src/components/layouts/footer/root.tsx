import { Container } from "../container";
import { Section } from "../section";

export const Root = () => {
  return (
    <footer>
      <Section className="bg-gradient-to-r from-gray-50 via-gray-300 to-gray-50">
        <Container>Footer</Container>
      </Section>
    </footer>
  );
};
